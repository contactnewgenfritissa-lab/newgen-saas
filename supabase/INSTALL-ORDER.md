# ترتيب تثبيت قاعدة البيانات

بما أن الجداول الأساسية لديك موجودة والصلاحيات الأساسية نُفذت، شغّل بالترتيب:

1. `005_business_modules.sql`
2. `006_reports_notifications.sql`
3. `007_optional_demo_seed.sql` اختياري

لا تشغّل `schema.sql` مجددًا على قاعدة البيانات الحالية.
